def reads(file_name):
 open('try.txt','r') as ['dictionary'['book','title','author','ISBN','price']]
return reads

def search_by_name(author_name):
    for book in books:
    if author name== author name:
        return search_by_name
    
def search_by_title(title):
    for book in books:
    if title== title:
        return search_by_title
    
def search_by_ISBN(ISBN):
    for book in books:
    if ISBN== ISBN:
        return search_by_ISBN
    
def search_by_price(price):
    for book in books:
        max price=float(input("Maximum price is",price))
        min price=float(input("Minimum price is",price))
    if price== price:
        return search_by_price
    
def general_search(search):
    search [search_by_price or search_by_ISBN or search_by_name]
    return books